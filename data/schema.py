"""
SQLAlchemy şemaları — Entity ayrımı (User, PreferenceProfile, VideoAsset, AnalysisJob, DetectionEvent, RedactionPlan).
Not: Geliştirmede SQLite, üretimde PostgreSQL geçişi düşünülerek yazılacak.
"""
# class User(Base): ...
# class PreferenceProfile(Base): ...  # {alcohol: bool, blood: bool, phobic: bool, violence: bool, obscene: bool, mode: "skip|blur|warn"}
# class VideoAsset(Base): ...
# class AnalysisJob(Base): ...
# class DetectionEvent(Base): ...
# class RedactionPlan(Base): ...
